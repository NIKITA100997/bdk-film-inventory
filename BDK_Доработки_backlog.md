# БДК — учёт плёнки: доработки по итогам код-ревью

Собрано по итогам сверки репозитория `NIKITA100997/bdk-film-inventory` с ТЗ (`БДК_Проект_учета_пленок.md`). Ниже — всё, что обсуждалось: найденные баги, архитектурные проблемы навигации, новые функции администрирования и открытые вопросы по готовности к полевому запуску.

---

## 1. Баги доступа (быстрые правки, `navConfig.ts`)

- [x] **Раздел "Материалы"** (карточка материала, дашборд остатков) сейчас доступен ролям `logist, nachalnik_tsekha, snabzhenets`. По ТЗ (5.5) должен быть доступен `operator_sklada, kladovshchik, logist` — именно тем, кому нужнее всего для повседневной работы.
- [x] **Раздел "Администрирование"** (Настройки, Справочники) сейчас доступен `admin, kladovshchik`. По ТЗ (5.6): доступ только у `logist`/`admin` (и IT). Кладовщик не должен туда попадать.

---

## 2. Архитектура навигации — объекты, а не действия

Текущая проблема: меню построено вокруг глаголов (отдельная кнопка на каждое действие), а не вокруг объектов (единица плёнки, позиция материала). Из-за этого один и тот же паттерн "скан ID → карточка → форма действия" продублирован четыре раза в разных экранах, а поиск/список остатков реализован параллельно в трёх местах.

### 2.1. Карточка единицы (новая, объединяющая)

Заменяет собой отдельные пункты меню **"Разделить рулон"**, **"Раскрой"**, **"Возврат"**, **"Размещение в ячейку"**, **"Перенести на склад"** (сейчас — `Split.tsx`, `Cut.tsx`, `Return.tsx`, `Place.tsx`, почти идентичные по структуре).

- [x] Один экран: сканирование QR **или** переход из объединённого списка материалов (2.2) → карточка конкретного ID.
- [x] Показывает: материал/цвет/толщина/производитель, текущие ширина/длина, статус, где сейчас (адрес или участок), УПД/паллета, историю (кто и когда с ней что делал).
- [x] Пул действий формируется по текущему статусу единицы:
  - На хранении → Разделить, Переместить (сменить ячейку), Выдать участку, Списать.
  - Выдан участку, цельнолистовые → Раскрой, Вернуть.
  - Выдан участку, окутка/щитовые → Вернуть.
  - Принят без адреса → Разместить (с автопредложением, как сейчас в `Place.tsx`).
- [x] Автоподбор адреса (`suggestLocation`) остаётся тем же вызовом, но встроен в карточку, а не дублируется в трёх разных страницах.
- [x] Старые роуты `/m/split`, `/m/cut`, `/m/return`, `/m/place` — либо редиректят на карточку конкретной единицы, либо удаляются из меню полностью в пользу входа через скан/список. *(удалены; выдача участку из карточки ведёт на `/m/issue` с предзаполнением, донор-предложение — обратно в карточку единицы)*

### 2.2. Материалы — один список вместо трёх

Сейчас параллельно существуют: `Dashboard.tsx` (без фильтров), `Search.tsx` (с фильтрами, но не в том же месте) и список единиц внутри `MaterialCard.tsx` — три разные реализации одного и того же "найти нужный рулон/штрипс".

- [x] Единая таблица с переключателем **"по позициям материала"** (агрегат) / **"по физическим единицам"**.
- [x] Общий набор фильтров: материал, цвет, толщина, производитель, статус, участок, ширина, минимальная длина.
- [x] Фильтр **"класс C, есть донор-кандидат"** — встроенная замена отдельной страницы `CuttingRecommendations.tsx`, которая сейчас изолирована от повседневного потока (донор и так уже всплывает автоматически в `/units/issue`, но отдельная страница рекомендаций требует, чтобы кто-то зашёл туда специально).
- [x] Кнопка **"Пересчитать ABC"** — здесь же, рядом с фильтрами, с датой последнего пересчёта (сейчас есть только на отдельной изолированной странице).
- [x] Клик по строке единицы → карточка единицы (2.1). Клик по строке позиции → карточка материала (агрегат, план/факт, разбивка по ширинам — уже реализовано, не трогаем).

### 2.3. Приёмка партии — сессия вместо одной страницы на рулон

- [x] Одна сессия приёмки на весь УПД, а не отдельный проход формы на каждый рулон.
- [x] Внутри — быстрый повторяющийся цикл: автокомплит материала/цвета/толщины/производителя (с запоминанием предыдущего ввода для рулонов того же цвета), ширина/длина с рулона, кнопка "Следующий" сразу открывает новую строку.
- [x] Автоподбор ячейки на этом же шаге — сейчас `Receive.tsx` не вызывает `suggestLocation` вообще, только опциональное текстовое поле адреса.
- [x] В конце сессии — сводка (сколько рулонов, сколько новых позиций материала создано) и печать всех этикеток одной кнопкой (частично уже есть — "Печать всех этикеток" в `Receive.tsx`, но привязано к одной приёмке одного цвета за раз).

---

## 3. Администрирование пользователей (отсутствует полностью)

Сейчас нет ни API, ни экрана — есть только `GET /auth/users` (список активных, используется исключительно для выбора участников инвентаризации). Пользователь заводится напрямую в БД.

- [x] `POST /users` — создание (ФИО, логин, временный пароль, роль, участок — если роль "начальник участка").
- [x] `PATCH /users/{id}` — смена роли/участка/статуса активности.
- [x] `POST /users/{id}/reset-password` — сброс пароля.
- [x] Деактивация вместо удаления (чтобы события в журнале не теряли автора).
- [x] Экран в администрировании: список с фильтром по роли → форма создания/редактирования.

---

## 4. Редактор макета этикетки (новая функция)

Сейчас макет и набор полей жёстко зашиты в `services/labels.py` как HTML-шаблон.

- [x] Список доступных для печати полей — всё из карточки единицы: ID, QR, материал, цвет, толщина, производитель, УПД, паллета, дата приёмки, код у поставщика, родная ширина, статус (цветная полоса), "из рулона №…".
- [x] **Ширина и длина в список по умолчанию не включать** — это принципиальное решение ТЗ (2.2): их отсутствие на бирке даёт всю экономию на переэтикетировании при разделении. Если всё же нужно оставить возможность их добавить — явное предупреждение в интерфейсе конструктора о потере актуальности.
- [x] Настройка порядка полей построчно (сверху вниз, с размером/жирностью шрифта) — не полноценный drag-n-drop холст, а список с сортировкой, этого достаточно для термопечати и надёжнее.
- [x] Физический размер этикетки — тоже параметр, не константа (сейчас 60×90 мм зашито).
- [x] Хранить макет как структуру (список полей + порядок + опции отображения), а не как HTML — `render_label_html` должен стать интерпретатором этой структуры, а не фиксированной функцией.
- [x] Превью и печать тестовой этикетки перед сохранением макета.
- [x] Место в администрировании — отдельный пункт "Макет этикетки", рядом со справочниками.

---

## 5. Прочие гибкие настройки

- [x] **Число ячеек на полке штрипсового стеллажа** — сейчас захардкожено (`CELLS_PER_STRIP_SHELF = 10` в `placement.py`), хотя по ТЗ должно "зависеть от ширины". Вынести в настройки — общий дефолт или per-стеллаж.
- [x] **Порог "давно не двигалась"** — отдельно от порога минимальной полезной ширины: сколько дней единица может пролежать без движения, прежде чем это станет сигналом на ревизию/инвентаризацию.
- [x] **Матрица доступа "роль × раздел меню"** — закрыто разделом 8.3: `navConfig.ts` по-прежнему хранит соответствие "пункт меню → права" в коде (как и предполагалось), но какие роли имеют какие права — теперь редактируемая таблица чекбоксов в "Роли и права", без правки кода.
- [x] **Шаблон текста сигнала о нехватке** (в разделе "Закупки") — сейчас формат зашит в логику; вынести в настройку по мере роста числа поставщиков/формулировок.

---

## 6. Открытый вопрос: донор-резка в два шага

Сейчас подтверждение донор-рекомендации — не атомарная операция: `/units/issue` только предлагает донора и логирует событие `Донор_предложен`; оператор вручную вызывает `/units/{id}/split`, а затем повторяет выдачу на результат. Рабочий вариант, но не "одно нажатие", как в макете.

- [x] Решено: объединили в один атомарный вызов — `POST /units/issue-donor-atomic` (резка + выдача за один шаг), кнопка «Выдать донора в 1 клик» в `Issue.tsx`.

---

## 7. Готовность к полевому запуску (напоминание, отдельно от функционала)

Не про новые функции, а про то, что нужно сделать перед реальной эксплуатацией — подробнее обсуждалось отдельно, здесь только чек-лист:

- [ ] Завести стеллажи (`Rack`) с реальным числом полок — без них автоподбор адреса ничего не предлагает.
- [ ] Завести базовые правила макрозонирования.
- [ ] Провести первичное внесение остатков через инвентаризацию (этап 2 roadmap).
- [ ] Завести реальных пользователей (зависит от раздела 3 — админки пользователей).
- [ ] Production-деплой: сборка фронтenda, процесс-менеджер для backend, реальный `JWT_SECRET`, PostgreSQL-сервер, смена пароля `admin/admin`.
- [x] Проверить печать на реальном принтере этикеток. *(Codex G500, размер 100×40 мм — печать нестабильна была из-за HTML-рендера и ненастроенного драйвера; решено переходом на серверный PDF (`reportlab`) + фиксация 100×40 мм как размера по умолчанию в драйвере Windows)*
- [ ] Проверить работу интерфейса на реальных ТСД/телефонах, покрытие Wi-Fi на складе. *(QR-сканирование камерой уже добавлено — но не проверено на реальном устройстве оператора)*

---

## 8. Навигация по функциям + гибкая ролевая модель

Раздел 2 закрыл дублирование внутри карточки единицы/материала, но не устранил его источник: навигация всё ещё собрана из двух параллельных деревьев (`mobileBlocks` и `desktopSections`), каждое из которых группирует пункты по роли/участнику, а не по функции. Отсюда конкретное дублирование в `navConfig.ts` прямо сейчас:

- **"Карточка единицы"** — три записи на один и тот же `/m/unit-card`: `m-unit-card-warehouse` (блок "Склад"), `m-unit-card-my-area` (блок "Мой участок"), плюс сам пункт не нужен вовсе как самостоятельный (см. 8.1).
- **"Карточка материала"** — две записи на `/materials`: `m-material-card` (мобильный блок "Общее") и `d-materials` (десктопный раздел "Материалы").
- **"Остатки"** — две записи на `/stock`: `m-stock` и `d-stock`.

Раздел 5 уже отмечал зависимость: пункт "матрица доступа роль×раздел" был отложен именно потому, что завязан на эту же структуру — теперь пересматриваем оба вопроса вместе.

### 8.1. Убрать "Карточка материала" и "Карточка единицы" как отдельные пункты меню

- [x] Удалить записи `m-material-card`, `d-materials`, `m-unit-card-warehouse`, `m-unit-card-my-area` из `navConfig.ts` — оба экрана остаются в роутинге (`/materials`, `/m/unit-card`), но не как пункты меню.
- [x] Единственные входы: клик по строке в "Остатки" (`MaterialsExplorer.tsx` уже это делает — просто убрать параллельные точки входа через меню), сканирование QR, и взаимные ссылки между экранами (8.4).

### 8.2. Единое навигационное дерево вместо `mobileBlocks` + `desktopSections`

- [x] Заменить два параллельных списка на одно дерево разделов, сгруппированное по функции, не по участнику/устройству: Остатки, Приёмка, Выдача участку, Инвентаризация, Планирование (Недельный план, План/факт), Заказы, Закупки, Отчёты, Администрирование.
- [x] У каждого пункта — один канонический `path`, без повторов на разные роли/блоки.
- [x] "Что у меня сейчас" (`MyArea.tsx`) — вместо отдельного пункта меню становится предустановленным фильтром внутри "Остатки" для роли "начальник участка" (по умолчанию фильтр "участок = мой"), а не отдельным экраном с собственным списком единиц.
- [x] Раскрой/резка/возврат/размещение — уже не пункты меню, доступны только как действия внутри карточки единицы (это уже сделано разделом 2.1 — здесь просто убеждаемся, что в новом дереве для них нет отдельных строк).
- [x] Мобильная/десктопная раскладка остаётся (адаптив под экран), но это вопрос **отображения** одного и того же дерева, а не двух разных деревьев с разным содержимым.

### 8.3. Гибкая ролевая модель — создаваемые роли, детальные права

Сейчас роль — фиксированный enum (`UserRole` в `models/users.py`), а видимость каждого экрана/эндпоинта — хардкод списка ролей (`require_roles("logist")` и так далее). Новую роль сейчас можно завести только правкой кода в нескольких местах сразу — ровно то, о чём просите.

**Каталог прав** (реализовано — 19 штук, а не 17: по факту в коде на момент реализации оказались ещё `orders.plan`, т.к. строки потребности заказа (п.4) — отдельная проверка `nachalnik_tsekha`, не входившая в исходный набросок каталога ниже, а `units.my_area`/`plans.manage` из наброска не соответствовали ни одной реальной проверке в коде и не были перенесены; `reports.view`/`sales_calculator.view` добавлены, чтобы разделы "Отчёты"/"Калькулятор заказа" — которые и раньше гейтились только на фронтенде — тоже управлялись через единый механизм прав):

| Право | Сейчас (жёстко) | Где |
|---|---|---|
| `units.receive` | `operator_sklada` | приёмка |
| `units.issue` | `operator_sklada` | выдача участку |
| `units.split` | `operator_sklada` | разделить рулон |
| `units.place` | `operator_sklada, kladovshchik` | разместить/переместить |
| `units.writeoff` | `operator_sklada, kladovshchik` | списать |
| `units.cut` | `operator_sklada, nachalnik_uchastka` | раскрой |
| `units.return` | `nachalnik_uchastka, kladovshchik, operator_sklada` | возврат |
| `materials.manage` | `logist` | справочники |
| `storage.manage` | `logist` | стеллажи, макрозонирование |
| `calc_settings.manage` | `logist` | настройки расчётов |
| `inventory.manage` | `logist, kladovshchik` | инвентаризация |
| `orders.plan` | `nachalnik_tsekha` | строки потребности заказа |
| `orders.manage` | `logist` | создание заказа |
| `orders.close` | `logist, kladovshchik` | закрытие заказа |
| `purchasing.manage` | `snabzhenets` | закупки |
| `labels.manage` | `logist` | макет этикетки |
| `users.manage` | `logist` | администрирование пользователей, ролей и прав |
| `reports.view` | `logist, nachalnik_tsekha` | отчёты (только фронт, как и раньше) |
| `sales_calculator.view` | `prodazhnik` | калькулятор заказа (только фронт, как и раньше) |

- [x] Новые таблицы: `roles` (id, code, name — создаваемые, не enum), `permissions` (фиксированный каталог выше), `role_permissions` (M2M), `user_roles` (M2M — роль у пользователя множественная).
- [x] Миграция данных: `User.role` конвертирован в 7 системных ролей-по-умолчанию с правами 1:1 по таблице выше, `role='admin'` → `is_superuser=true` — поведение не изменилось в момент миграции.
- [x] `require_roles(...)` на бэкенде заменён на `require_permission(...)` во всех 9 файлах API — проверяет объединение прав по всем ролям пользователя.
- [x] `admin` — отдельный флаг-суперпользователь (`is_superuser`), не через таблицу прав.
- [x] `navConfig.ts`: каждый пункт вместо `roles: UserRole[]` получил `permissions?: string[]` — видимость по эффективным правам из `/auth/me`.
- [x] Экран администрирования **"Роли и права"** (`RoleAdmin.tsx`): список ролей → создать новую (только название) → чекбоксы прав по разделам (Склад, Планирование, Заказы, Закупки, Администрирование) → в `UserAdmin.tsx` роль стала мультивыбором + отдельный чекбокс "Суперпользователь".
- [x] `area` остался отдельным атрибутом пользователя, не частью прав.

### 8.4. Взаимные ссылки материал ↔ единица

- [x] В карточке материала список единиц (уже есть) — клик по строке ведёт в карточку единицы.
- [x] В карточке единицы — строка "Материал: …" становится ссылкой на карточку соответствующей позиции материала (сейчас просто текст `skuLabel(unit.material_sku)`).

### 8.5. Кнопка "+ Новое" на экране "Остатки"

Раз добавление позиций и единиц не сосредоточено в одном явном месте (позиция создаётся неявно через приёмку, единица — только через приёмку или инвентаризацию), на "Остатках" нужна прямая точка входа с двумя вариантами:

- [x] **"Материал"** — форма создания позиции без физической единицы (материал/цвет/толщина/производитель/код у поставщика/родная ширина — те же поля, что в `MaterialSku`). Пригодится, чтобы завести номенклатуру заранее, до фактической поставки (например, под будущую позицию недельного плана).
- [x] **"Единица плёнки"** — регистрация одного физического рулона/штрипса вне формальной сессии приёмки (УПД/паллета — необязательные поля, можно отметить "без документа"). По сути — переиспользование той же формы, что уже есть в ветке "излишек" сценария инвентаризации (`Inventory.tsx`, когда ID не читается) — тот же автокомплит справочников, автоподбор ячейки (4.2), печать бирки. Не подменяет собой сессионную приёмку по УПД (2.3) — для одиночных случаев без открытия/закрытия сессии.
- [x] Права — по аналогии с приёмкой (`units.receive`); решил без отдельного права — `require_roles("operator_sklada", "logist")`, та же логика, что и у остальной складской записи.

---

## 9. Пользовательский опыт — по итогам живого тестирования (backend поднят локально, эндпоинты проверены запросами)

### 9.1. Макет этикетки — не баг, а отсутствующий drag-and-drop

Порядок полей в `LabelTemplateAdmin.tsx` меняется только кнопками "Вверх"/"Вниз" — библиотеки перетаскивания (dnd-kit/react-beautiful-dnd) не подключено. API проверен напрямую (Postgres + миграции + сиды подняты локально) — оба эндпоинта (`/label-template`, `/label-template/available-fields`) отдают корректные данные, проблема не в загрузке.

- [x] Заменить кнопки на настоящий drag-and-drop (например, `@dnd-kit/sortable` — уже стандарт в антд-проектах).

### 9.2. Справочники: автосоздание работает, но не видно и не защищено от опечаток

`find_or_create_sku` (`services/dictionaries.py`) при приёмке действительно создаёт цвет/производителя из введённого текста — похода в админку не требуется технически. Но:

- [x] В `DictAutoComplete.tsx` нет визуальной подсказки "Создать «…»" при отсутствии совпадений — пользователь не понимает, что делать, кроме как искать администратора.
- [x] **Полностью отсутствует нечёткое сравнение и предупреждение о похожих значениях**, обещанное разделом 2.1a ТЗ ("Похоже на «Дуб беленый» — это тот же цвет?"). Сейчас любая опечатка при вводе на приёмке молча создаёт дубль справочника — именно тот сценарий, ради защиты от которого справочники и вводились. Приоритетная правка. *(клиентский эквивалент на Левенштейне, utils/similarity.ts, без похода на сервер)*
- [x] Дать пользователю явно увидеть последствие: "Значение «…» не найдено — при сохранении будет создано новое" вместо тихого создания без подтверждения. *(вариант "Создать «…»" прямо в выпадающем списке)*

### 9.3. Отказ в доступе — без сообщения

`RoleGuard.tsx`: при нехватке прав — тихий `<Navigate to="/" replace />`, никакого сообщения. Пользователь, перешедший по старой ссылке или после смены роли/прав, просто оказывается в другом месте без объяснений.

- [x] Показывать явное сообщение ("У вас нет доступа к этому разделу") перед или вместо редиректа.

### 9.4. Разрыв сессии — потеря контекста

`client.ts`: при 401 токен тихо удаляется, редирект на `/login` без сообщения "сессия истекла" и без сохранения текущего экрана/несохранённого ввода (например, посреди сессии приёмки на партию из 9 рулонов). После повторного входа — всегда на `/`, не туда, где остановились.

- [x] Сообщение о причине редиректа на логин.
- [x] Возврат на исходный экран после повторного входа (сохранить `location.pathname` перед редиректом).
- [ ] Для длинных форм (приёмка сессией, создание плана) — рассмотреть автосохранение черновика в форме, чтобы разрыв соединения не терял ввод. *(не делал — самостоятельная задача крупнее остальных пунктов 9.4, требует отдельного решения по хранилищу черновиков)*

### 9.5. Пустые состояния — несогласованы по разделам

Есть заботливые примеры (`Purchasing.tsx` — "Нехватки не обнаружены" с иллюстрацией; `Search`/`MyArea` — осмысленный `emptyText`), но не везде: `WeeklyPlan.tsx` при отсутствии планов показывает просто пустое место без подсказки "создайте первый план".

- [x] Пройтись по всем разделам и унифицировать пустые состояния: что это, зачем, какое действие ожидается первым. *(общий компонент EmptyHint; применён в WeeklyPlan.tsx, PlanFact.tsx, Orders.tsx — местам с уже хорошим emptyText не потребовалось правок)*

### 9.6. Таблицы — нет сортировки и экспорта

Ни в одной таблице (`MaterialsExplorer`, `Reports`, `Orders`, `PlanFact`) нет сортировки по столбцам и выгрузки в CSV/Excel — базовый ожидаемый минимум для отчётных таблиц в учётных системах (1С, SAP, Odoo).

- [x] Добавить `sorter` на числовые/датовые колонки как минимум в "Остатки" и "Отчёты".
- [x] Кнопка "Экспорт в Excel" хотя бы на дашборде остатков и в отчётах — частый запрос от бухгалтерии/руководства, который иначе решается вручную копированием. *(CSV с BOM и ";" — открывается в Excel как есть, без отдельной библиотеки)*

### 9.7. Нет глобального поиска

Чтобы найти единицу/материал/заказ, нужно точно знать, в какой раздел идти. ERP-системы обычно дают одну строку поиска в шапке, которая ищет по всему (ID единицы, номер заказа, название материала) и ведёт сразу в нужную карточку.

- [x] Строка поиска — на самом видном месте экрана "Остатки" (не среди прочих фильтров, а отдельно, сверху). Логика: если введено число — трактуем как ID единицы и сразу открываем карточку единицы, минуя список; если текст — фильтруем список по материалу/цвету, как обычный фильтр. Это единственный быстрый путь попасть в карточку единицы по известному номеру (например, продиктованному по телефону) — без него, раз "Карточка единицы" убрана из меню (8.1), у пользователя, знающего ID, но не имеющего физического доступа к QR, нет способа быстро её найти.
- [x] Вынести ту же строку в шапку `AppLayout.tsx` глобально (видна на любом экране), а не только на "Остатки" — особенно полезно с учётом того, что "Карточка единицы" и "Карточка материала" (8.1) больше не в меню. *(число → сразу карточка единицы; текст → переход на "Остатки" с запросом через `location.state`, `MaterialsExplorer` его подхватывает — строка на самой "Остатках" остаётся на месте, шапка не дублирует фильтры, а даёт быстрый вход из любого экрана)*
